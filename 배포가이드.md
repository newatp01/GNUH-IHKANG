# gnuh-ihkang.kr / gnuh-ihkang.com 로 웹사이트 올리기

가비아에서 `gnuh-ihkang.kr`, `gnuh-ihkang.com` 두 도메인을 구매하셨으니, 이제 ②③④ 단계만 남았습니다.
전부 웹 브라우저 클릭만으로 진행할 수 있습니다.

전체 흐름: ~~① 도메인 구매~~ (완료) → **② GitHub에 코드 올리기 → ③ Render에 배포 → ④ 가비아에서 도메인 연결**

---

## ② GitHub에 코드 올리기 (무료)

1. https://github.com 에서 무료 계정을 만듭니다.
2. 오른쪽 위 `+` 버튼 → `New repository` 클릭 → 이름을 `gnuh-ihkang` 으로 입력 → `Create repository`.
3. 저장소 페이지에서 `Add file` → `Upload files` 클릭.
4. 압축을 푼 이 폴더(nurse_site) 안의 파일과 폴더를 전부 끌어다 놓습니다. (app.py, requirements.txt, Procfile, render.yaml, templates 폴더, static 폴더 등)
5. 아래 `Commit changes` 버튼을 눌러 업로드를 완료합니다.

## ③ Render에 배포하기

1. https://render.com 에서 회원가입합니다. (GitHub 계정으로 바로 로그인 가능)
2. 대시보드에서 `New +` → `Web Service` 클릭.
3. 방금 만든 `gnuh-ihkang` 저장소를 선택하고 `Connect`.
4. 설정 화면에서:
   - Name: `gnuh-ihkang` (원하는 이름)
   - Runtime: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app`
   - Instance Type: `Starter` ($7/월, 항상 켜져 있는 요금제로 선택해주세요. 무료 요금제는 데이터가 계속 유지되지 않습니다)
5. `Environment` 메뉴에서 환경변수를 추가합니다.
   - `DATA_DIR` = `/var/data`
   - `SECRET_KEY` = 아무 임의의 긴 문자열 (예: `kjh2026-secret-key-change-me`)
6. `Disks` 메뉴에서 디스크를 추가합니다. (데이터베이스와 회의록 파일을 안전하게 보관하려면 꼭 필요합니다)
   - Name: `data`
   - Mount Path: `/var/data`
   - Size: `1GB`
7. `Create Web Service` 클릭 → 몇 분 기다리면 배포가 끝납니다. Render가 만들어준 임시 주소(예: `https://gnuh-ihkang.onrender.com`)로 먼저 접속해서 로그인부터 각 메뉴까지 정상 작동하는지 확인하세요.

## ④ 가비아에서 도메인 연결하기

**먼저 Render에서 두 도메인을 모두 등록합니다.**

1. Render 대시보드 → 방금 만든 서비스 → `Settings` → `Custom Domains` → `Add Custom Domain`.
2. `gnuh-ihkang.com` 입력 후 추가. 같은 방식으로 `gnuh-ihkang.kr` 도 추가합니다. (원한다면 `www.gnuh-ihkang.com` 처럼 www 버전도 함께 추가할 수 있습니다)
3. 도메인마다 Render가 등록해야 할 값을 알려줍니다. 보통 아래 두 가지 중 하나입니다.
   - 루트 도메인(예: `gnuh-ihkang.com`, `gnuh-ihkang.kr` 그 자체) → **A 레코드**, 값은 Render가 알려주는 IP 주소
   - `www` 같은 하위 도메인 → **CNAME 레코드**, 값은 Render가 알려주는 `xxxxx.onrender.com` 형태의 주소
   - 화면에 표시되는 값을 그대로 복사해두세요. (직접 추측해서 입력하지 말고 Render가 알려주는 값을 그대로 사용해야 합니다)

**가비아에서 DNS 레코드를 추가합니다.** (.kr, .com 두 도메인 모두 같은 방법입니다)

1. 가비아(gabia.com) 로그인 → 오른쪽 위 `My가비아` 클릭.
2. `이용중인 서비스 > 도메인` 메뉴에서 `DNS 관리툴` 또는 `DNS 정보` 클릭.
3. 연결할 도메인(`gnuh-ihkang.com` 또는 `gnuh-ihkang.kr`) 옆의 `관리` 또는 `설정` 버튼 클릭.
4. `레코드 추가`를 눌러 Render에서 확인한 값을 입력합니다.
   - A 레코드: 타입 `A`, 호스트에 `@` 입력(루트 도메인 의미), 값에 Render가 준 IP 주소 입력
   - CNAME 레코드: 타입 `CNAME`, 호스트에 `www` 등 입력, 값에 Render가 준 주소를 입력하되 **끝에 마침표(.)를 붙여야** 정상 등록되는 경우가 많습니다 (예: `xxxxx.onrender.com.`)
5. 저장을 누릅니다. `gnuh-ihkang.com`, `gnuh-ihkang.kr` 두 도메인 모두 같은 방식으로 반복합니다.
6. 보통 10분~수 시간 안에 연결됩니다. 연결이 확인되면 Render가 무료 SSL 인증서(https)를 두 도메인 모두에 자동으로 발급해줍니다.

연결이 끝나면 `https://gnuh-ihkang.com` 과 `https://gnuh-ihkang.kr` 둘 다 같은 사이트로 접속됩니다. 굳이 하나로 통일하고 싶으시면 나중에 Render의 리다이렉트(redirect) 설정으로 한쪽을 다른 쪽으로 자동 이동시킬 수도 있는데, 지금 당장은 필요 없습니다.

---

## 참고

- 접속 비밀번호와 이름 목록은 기존과 동일합니다. (사이트 비밀번호: dnfldusrn11 / 이름: 강정훈, 문병선, 이우제, 김하은, 이혜란)
- 코드나 디자인을 나중에 수정하고 싶다면, GitHub 저장소의 파일을 다시 업로드(덮어쓰기)하면 Render가 자동으로 다시 배포합니다.
- 가비아 DNS 관리 화면의 정확한 메뉴 이름은 시기에 따라 조금 다를 수 있습니다. "DNS", "네임서버", "호스트 설정" 등의 이름을 찾아보시면 됩니다.
- 진행하다가 막히는 화면이 나오면 스크린샷을 보여주시면 다음 단계를 같이 확인해드릴 수 있습니다.
